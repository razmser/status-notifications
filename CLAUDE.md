## Agent skills

### Issue tracker

Issues live as local markdown under `.scratch/<feature>/` (no PR triage surface).
See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped 1:1 to default status strings.
See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root.
See `docs/agents/domain.md`.
